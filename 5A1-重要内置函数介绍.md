# 重要内置函数介绍

时间：`2026年9月11日`

## 1. 输入输出：`print()`/ `input()` 函数

### `print()` 函数

内置函数的总体介绍见[函数笔记](./5-函数.md)

- `print()` 函数的作用是让计算机显示参数的内容在屏幕的**终端**上
- `print()` 可一次输出多个变量，且变量类型不必一致；如，`print(123, "xyz", True)`
  - `sep` 参数：指定变量之间的分隔符，默认**空格**；可以改成其他字符串
- `end` 参数：指定 `print()` 结束时附加的字符，**默认是换行符 `\n`**（所以每次 print 都自动换行）；改成其他字符串可以让输出不换行

    ```python
    print("Hello", end=" ")   # 以空格结尾，不换行
    print("World", end="")    # 以空结尾，不换行，和下次的输出连在一起
    ```

### `input()` 函数

- `input()` 函数的作用是接收用户在键盘上输入的内容，并把它**作为字符串（str）返回**
  - 注意：无论用户输入的是数字还是字母，`input()` 返回的都是字符串；若要进行数值运算，需先用 `int()` 或 `float()` 转换成数字
- **语法**：`input([prompt])`，其中 `prompt` 是可选参数，会先显示给用户作为提示信息
- 示例：

    ```python
    name = input("请输入你的名字：")
    print(f"你好, {name}!")

    age = int(input("请输入你的年龄："))
    print(f"明年你 {age + 1} 岁")
    ```

---

## 2. 数值计算函数

### `abs(x)`

- 作用：返回 `x` 的**绝对值**；如果 `x` 是复数，返回复数的**模**（即 $\sqrt{real^2 + imag^2}$）
- 参数 `x` 可以是整数、浮点数或复数；返回值类型与常见约定：整数 → 整数，浮点数 → 浮点数，复数 → **浮点数**（模长）

    ```python
    abs(-5)        # 输出: 5
    abs(3.2)       # 输出: 3.2
    abs(3 + 4j)    # 输出: 5.0（复数的模，注意是浮点数）
    ```

### `divmod(a, b)`

- 作用：一次运算同时返回 `a` 除以 `b` 的**商和余数**，结果是一个**元组** `(a // b, a % b)`
  - 等价关系：`divmod(a, b)` 约等于 `(a//b, a%b)`；
  - 常用于"进位/分组"类问题，如把秒数换算成分钟和剩余秒数
- 注意：对负数同样遵循"余数符号与除数一致"的规则（与 `%` 一致）

    ```python
    divmod(10, 3)     # 输出: (3, 1)
    divmod(10.5, 2)   # 输出: (5.0, 0.5)
    divmod(-10, 3)    # 输出: (-4, 2)

    # 典型用法：秒数 → 分:秒
    m, s = divmod(125, 60)    # m=2, s=5
    ```

### `pow(x, y[, z])`

- 作用：返回 `x` 的 `y` 次幂，即 $x^y$；与运算符 `**` 等价
- 可选第三参数 `z`：`pow(x, y, z)` 返回 $(x^y) \mod z$，即"模幂运算"，比先算幂再取模**高效得多**（大数运算时不会先产生天文数字）

    ```python
    pow(2, 10)        # 输出: 1024
    pow(2, pow(2, 2)) # 输出: 16（先算 2**2=4，再算 2**4）
    pow(2, 3, 5)      # 输出: 3（(2**3) % 5 = 8 % 5）
    ```

### `round(n[, ndigits])`

- 作用：对 `n` 进行舍入；第二个参数 `ndigits` 可选，指定保留的小数位数，**省略时返回整数（int）**
- Python 3 的 `round` **不是真正的"四舍五入"**，而是 **"银行家舍入"（四舍六入五取偶 / round half to even）**：当恰好在中间值（.5）时，向**最近的偶数**靠拢
  - 这是常见考点/陷阱：`round(0.5)` 和 `round(1.5)` 都返回 `0` 和 `2` 看似正常，但 `round(2.5)` 返回 `2` 而不是 `3`
  - 另外浮点数本身有精度误差（见[笔记1](./1-基本数据类型和变量.md)浮点数一节），也会让个别"五入"案例看起来反常，如 `round(2.675, 2)` 得到 `2.67`

    ```python
    round(10.6)      # 输出: 11
    round(0.5)       # 输出: 0（五取偶）
    round(1.5)       # 输出: 2
    round(2.5)       # 输出: 2（不是 3！）
    round(3.14159, 2)  # 输出: 3.14
    ```

---

## 3. 对组合类型（容器）操作的函数

### `all(x)` / `any(x)`

- 两者都接收一个**可迭代对象**（列表、元组、字符串、生成器等），对其中每个元素做**真值判断**（truthy / falsy），返回布尔值
  - False 的值只有：`False`、`0`、`0.0`、`""`、`[]`、`()`、`{}`、`None`；**其余全部视为 True**
- `all(x)`：**所有**元素都为真才返回 `True`，否则 `False`；**`x` 为空时返回 `True`**
- `any(x)`：**任一个**元素为真就返回 `True`，否则 `False`；**`x` 为空时返回 `False`**
- 两者都是**短路求值**：`all` 遇到第一个假元素、`any` 遇到第一个真元素就立即停止

### `reversed(r)`

- 作用：返回可迭代对象 `r` 的**逆序形式**
- 注意：返回的**不是列表，而是一个迭代器**（`reversed` 对象），不修改原对象；想立刻看到完整结果要用 `list()` 包一层；想倒着遍历则可直接用于 `for` 循环

    ```python
    reversed([1, 2, 3])          # 输出: <reversed object>（迭代器）
    list(reversed([1, 2, 3]))    # 输出: [3, 2, 1]
    list(reversed("abc"))        # 输出: ['c', 'b', 'a']

    for i in reversed([1, 2, 3]):
        print(i, end=" ")        # 输出: 3 2 1
    ```

### `sorted(x[, key][, reverse])`

- 作用：对可迭代对象 `x` 中的元素**排序**，返回一个**新的列表**，**不修改原对象**；默认从小到大（升序）
- 参数：
  - `key`：传入一个函数，**按"每个元素调用该函数的结果"来比较**，如 `key=str.lower` 忽略大小写排序
  - `reverse=True`：改为降序
- 与**列表方法** `list.sort()` 的区别：
  - `sorted(x)` 是**内置函数**，适用于**任何可迭代对象**，返回新列表，原对象不变
  - `x.sort()` 是**列表专属方法**，**原地排序**，返回 `None`

    ```python
    sorted([3, 1, 2])              # 输出: [1, 2, 3]
    sorted([3, 1, 2], reverse=True)  # 输出: [3, 2, 1]
    sorted("python")               # 输出: ['h', 'n', 'o', 'p', 't', 'y']
    sorted(['B', 'a', 'C'])        # 输出: ['B', 'C', 'a']（默认按 Unicode 编码，大写在前）
    sorted(['B', 'a', 'C'], key=str.lower)  # 输出: ['a', 'B', 'C']

    nums = [3, 1, 2]
    nums.sort()    # nums 本身变成 [1, 2, 3]，表达式值为 None
    ```

### `sum(x[, start])`

- 作用：对可迭代对象 `x` 中的元素**求和**；可选参数 `start` 为初始值（默认 0），结果等于 `start + x 中所有元素之和`
- 注意：元素必须是**数字**（int/float/complex），不能对字符串用 `sum` 拼接（字符串拼接用 `"".join()`）

    ```python
    sum([1, 2, 3])          # 输出: 6
    sum([1, 2, 3], 10)      # 输出: 16
    sum(range(1, 101))      # 输出: 5050（1 到 100 求和）
    sum([])                 # 输出: 0
    ```

---

## 4. 表达式求值与序列生成函数

### `eval(s)`

- 作用：把字符串 `s` **当作 Python 表达式**来求值，并**返回结果**
- **只能处理"表达式"（有值的东西）**，不能处理"语句"（如赋值、循环）——那是 `exec` 的活
- **安全警告**：`eval` 会真实执行字符串里的代码，**绝不能用于处理不可信来源的输入**

    ```python
    eval("1 + 2")           # 输出: 3
    eval("max(1, 5, 3)")    # 输出: 5
    eval("[1, 2] + [3]")    # 输出: [1, 2, 3]
    eval("3 > 2")           # 输出: True

    # 常见用法：把 input() 读入的字符串直接变成对应类型的值
    x = eval(input("请输入："))   # 输入 123 → int；输入 1.5 → float；输入 [1,2] → list
    ```

### `exec(s)`

- 作用：把字符串 `s` **当作 Python 语句**来**执行**；执行后**返回 `None`**
- 与 `eval` 的核心区别（对比记忆）：
  - `eval`：求**表达式**的值，**必须有返回值**
  - `exec`：执行**语句**（赋值、循环、函数定义等都可以），**不返回值**

    ```python
    exec("a = 1 + 2")    # 执行赋值语句
    print(a)             # 输出: 3

    exec("print('hi')")  # 输出: hi（表达式也能放，只是不取返回值）

    eval("a = 1")        # 报错: SyntaxError（赋值是语句，不是表达式）
    ```

### `range(a, b[, s])`

- 作用：生成一个**整数序列**——从 `a` 开始、到 `b` **结束但不包含 `b`**、步长为 `s`（默认 1）
- 三种写法：
  - `range(n)`：0 到 n-1
  - `range(a, b)`：a 到 b-1
  - `range(a, b, s)`：a 起步长 s 到 b（不含 b）；`s` 可为负数（倒着走）
- 注意：`range()` 返回的是**惰性的 range 对象**（不占内存，用到才算），想看完整列表要 `list(range(...))`；
- 最常与 `for` 循环搭配，控制循环次数

    ```python
    list(range(5))          # 输出: [0, 1, 2, 3, 4]
    list(range(1, 6))       # 输出: [1, 2, 3, 4, 5]（不含 6）
    list(range(0, 10, 2))   # 输出: [0, 2, 4, 6, 8]
    list(range(5, 0, -1))   # 输出: [5, 4, 3, 2, 1]（步长为负）

    for i in range(3):      # 循环体执行 3 次，i 依次为 0, 1, 2
        print(i)
    ```

---

## 5. 对象身份函数

### `id(object)`

- 作用：返回对象的**身份标识**（identity），一个整数；该值在对象的**整个生命周期内唯一且不变**，**两个生命周期不重叠的对象可能拥有相同的 `id`**
- 主要用途：
  - 判断两个变量是否指向同一个对象，也是 `is` 运算符的判定依据（`a is b` 等价于 `id(a) == id(b)`，见[笔记1](./1-基本数据类型和变量.md)“变量比较”一节）
  - 区分**原地修改**与**新建对象**，例如 `lst += [x]` 后 `id` 不变，`lst = lst + [x]` 后 `id` 改变
- 注意：
  - 不能把 `id` 保存下来当作长期标识符，对象被回收后其编号可能被新对象复用
  - 不要用不可变字面量的 `id` 或 `is` 做值比较，小整数缓存、字符串驻留、编译期常量合并都会影响结果


