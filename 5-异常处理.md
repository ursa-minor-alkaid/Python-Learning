# 异常处理

时间：`2026年9月6日`

## 1. 异常及异常处理介绍

- 异常（Exception）：
  - 程序在**运行过程中**发生的错误。一旦发生异常且未被处理，程序会立即中断，并在终端显示错误信息（Traceback）
  - **异常 ≠ 语法错误**：语法错误在程序运行前就会被解释器发现；而**异常发生在程序运行过程中，往往取决于具体的数据或环境（如用户输入、文件是否存在）**
- 异常处理目的：**在异常发生时捕获它，执行预设的处理逻辑**，让程序不因一个可预见的错误而整体崩溃
- 注意：异常处理与[笔记4](./4-条件语句与循环语句.md)中的控制语句（`break`/`continue`）不同。控制语句操纵的是**循环**的走向，异常处理改变的是**出错时**的程序走向

---

## 2. 异常处理语句

### `try...except` 语句

#### 基础语法

```python
try:
    可能发生异常的代码
except:
    发生异常时执行的代码
```

- 把可能出错的代码放在 `try` 中：一旦发生异常，就**跳过 `try` 中剩余的代码**，转而执行 `except` 中的代码；
- 若没有异常，`except` 中的代码不会执行
- `try` 和 `except` 后都要有冒号 `:` 和缩进（参见[笔记4A](./4A-冒号、缩进和语句顺序.md)）

**示例：**

```python
try:
    result = 10 / 0
except:
    print('除数不能为零')

print('程序没有崩溃，继续运行')
# 输出: 除数不能为零
#       程序没有崩溃，继续运行
```

#### 复杂情况

**捕获特定异常：**

- 裸 `except:` 会捕获所有异常，可能把真正的问题掩盖掉；更推荐指明要捕获的异常类型

    ```python
    try:
        result = 10 / 0
    except ZeroDivisionError:
        print('除数不能为零')
    ```

- 若发生的异常类型与 `except` 指定的类型不符，异常不会被捕获，程序仍会崩溃
  - 程序崩溃时会抛出异常的类型

**捕获多个异常：**

```python
try:
    number = int(input('请输入一个整数：'))
    result = 10 / number
except (ValueError, ZeroDivisionError):
    print('输入不合法')
```

**获取异常信息：**

- 用 `except Exception as e` 把异常对象赋值给变量 `e`，可以查看具体的错误描述

```python
try:
    result = 10 / 0
except ZeroDivisionError as e:
    print(f'发生了异常: {e}')
# 输出: 发生了异常: division by zero
```

### `else` / `finally` 子句

#### `try ... else`

- `else` 子句**仅在 `try` 中没有发生任何异常时**才执行
- 注意与循环的 `for...else` 同构：循环的 `else` 表示“没有被 `break` 打断”；`try` 的 `else` 表示“没有发生异常”（对比参见[笔记4](./4-条件语句与循环语句.md)）

    ```python
    try:
        print('try 中的代码正常执行')
    except:
        print('出错了')
    else:
        print('没有发生异常，才执行这里')
    ```

#### `finally`

- `finally` 子句**无论如何都会执行**：无论是否发生异常、异常是否被捕获
- 适合放置清理工作（如关闭文件、断开连接）

    ```python
    try:
        result = 10 / 0
    except ZeroDivisionError:
        print('除数不能为零')
    finally:
        print('无论如何都会执行')
    ```

### 主动抛出异常：`raise`

- `raise` 用于**主动抛出一个异常**，通常用于在代码中检查到不合法状态时发出信号

    ```python
    age = int(input('请输入年龄：'))

    if age < 0:
        raise ValueError('年龄不能为负数')
    # 抛出: ValueError: 年龄不能为负数
    ```

  - 注意，这里返回的不是自定义异常（这是一个类），而是内置的异常 + 异常信息
  - 异常信息：`ErrorType('message')`

---

## 3. 附：常见异常类型

|异常类型|说明|触发示例|
|-----|------|------|
|`ZeroDivisionError`|除数为零|`10 / 0`|
|`ValueError`|值不合法|`int('abc')`|
|`TypeError`|类型不匹配|`'a' + 1`|
|`IndexError`|索引越界|`[1, 2][5]`|
|`KeyError`|字典中不存在该键|`d['不存在的键']`|
|`NameError`|使用未定义的变量|`print(undefined_var)`|
|`FileNotFoundError`|文件不存在|`open('不存在的文件.txt')`|
|`ImportError`|导入模块失败|`import 不存在的模块`|
