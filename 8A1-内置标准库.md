# 内置标准库

时间：`2026年9月11日`

## 1. 标准库基本介绍

### 标准库介绍

- **定义**：标准库（Standard Library）是随 Python 解释器一同安装、开箱即用的模块集合，由 Python 官方开发团队维护，是 Python 发行版的一部分
  - **"Batteries Included"（自带电池）**：Python 的设计哲学——核心语言保持精简，但通过标准库提供大量常用功能，绝大多数日常任务**不需要安装任何东西**就能完成
  - **规模**：Python 3.x 标准库包含 **200+ 个模块**，官方文档称为 "The Python Standard Library"，覆盖从文本处理到网络通信的各个方面
- **使用方式**：无需 `pip install`，直接 `import` 即可（与第三方库的最大区别，详见 `8A2-第三方库.md`）
- **和内置函数/内置类型的区别**：`print()`、`len()`、`list` 等属于**内建（builtins）**，连 `import` 都不需要；标准库模块则必须显式导入后才可用

### 标准库的位置

- 标准库随 Python 解释器一同安装，位于 **Python 安装目录下的 `Lib` 目录**中（例如 `C:\Program Files\Python312\Lib\`）
  - 如何找到确切位置：查看解释器的搜索路径

    ```python
    import sys
    print(sys.path)   # 其中指向标准库的目录就在安装目录的 Lib 下
    ```

- 标准库中的大多数模块就是普通的 `.py` 源文件，打开即可看到具体实现
- 例外：少数模块（如 `sys`、`time`）由 C 语言实现，编译后以 `.pyd`（Windows）/ `.so`（Linux/macOS）形式存在或直接内置于解释器中，看不到 Python 源码

---

## 2. 重要内置标准库

### Random 模块

**介绍**：`random` 是 Python **标准库**（随解释器自带，无需 pip 安装），用于生成各种分布的随机数。

```python
import random
```

#### 伪随机数介绍

- `random` 库生成的不是**真随机数**，而是**伪随机数**（Pseudo-Random Number）
  - 由确定的算法（Python 使用 **梅森旋转算法 Mersenne Twister**）从初始值出发推算出一串序列
  - 序列看起来无规律、不可预测，但只要初始值相同，产生的序列就完全相同——即**可复现**
- **种子（seed）**：算法的初始值
  - 不手动设置时，默认用**当前系统时间**作种子，所以每次运行结果不同
  - 固定种子后，随机序列就固定，常用于：实验复现（保证结果可重复）、单元测试
  - 种子示例：

    ```python
    import random
    random.seed(10)
    print(random.random())   # 0.5714075646146815
    random.seed(10)
    print(random.random())   # 0.5714075646146815 —— 种子相同，结果相同
    ```
- 真随机数的来源是物理现象（放射性衰变、热噪声等）；若需要密码学安全的随机数，应使用 `secrets` 模块而非 `random`

#### 常用函数

| 函数 | 描述 |
|---|---|
| `seed(a=None)` | 初始化随机数种子，默认值为当前系统时间 |
| `random()` | 生成一个 `[0.0, 1.0)` 之间的随机小数 |
| `randint(a, b)` | 生成一个 `[a, b]` 之间的随机整数（**两端都含**） |
| `randrange(start, stop[, step])` | 生成一个 `[start, stop)` 之间以 `step` 为步长的随机整数（**不含 stop**） |
| `uniform(a, b)` | 生成一个 `[a, b]` 之间的随机小数 |
| `choice(seq)` | 从序列类型（例如列表）中随机返回一个元素 |
| `shuffle(seq)` | 将序列类型中元素随机排列，返回打乱后的序列 |
| `sample(seq, k)` | 从 `seq` 中随机选取 k 个元素，以列表类型返回 |
| `getrandbits(k)` | 生成一个 k 比特长度的随机整数 |

- `randint(a, b)` 闭区间、`randrange(start, stop)` 半开区间，与 `range()` 的取边界方式一致
- `shuffle(seq)` 实际是**原地打乱**（直接修改原列表，返回 `None`），不能写成 `new_list = shuffle(old_list)`
- `sample(pop, k)` 是**不放回**抽样（k 个元素互不重复），且不会修改原序列

### Time 模块

**介绍**：`time` 是 Python 标准库，用于处理时间。时间在 Python 中有三种表示方式：

1. **时间戳（timestamp）**：从 1970-01-01 00:00:00（UTC 纪元）起算的秒数（浮点数），如 `1757500000.123`
2. **结构化时间（struct_time）**：UTC 或本地时间的 9 元素元组（年、月、日、时、分、秒、星期等）
3. **字符串时间**：格式化后的可读字符串，如 `"2026-09-11 15:30:00"`

```python
import time
```

**常用函数**：

| 函数 | 描述 |
|---|---|
| `time.sleep(s)` | 让程序**暂停** `s` 秒（参数可以是小数），最常用的考点 |
| `time.time()` | 返回当前时间戳（自 1970 年起的秒数），常用于**计时**：前后各取一次相减得运行时长 |
| `time.ctime()` | 把时间戳或当前时间转为可读字符串，如 `'Thu Sep 11 15:30:00 2026'` |
| `time.gmtime([secs])` | 把时间戳转为 **UTC** 结构化时间（struct_time）；不传参则用当前时间 |
| `time.localtime([secs])` | 把时间戳转为**本地**结构化时间（中国为 UTC+8） |
| `time.perf_counter()` | 高精度性能计时器，**测量短时间间隔首选**（比 `time.time()` 精度高、不受系统时钟调整影响） |
| `time.strftime(format[, t])` | 按格式把结构化时间**格式化**为字符串 |
| `time.strptime(string, format)` | `strftime` 的逆操作：字符串 → 结构化时间 |
| `time.mktime(t)` | 结构化时间 → 时间戳 |

**`strftime` 格式控制符**（高频考点，需记忆）：

| 符号 | 含义 | 示例 |
|---|---|---|
| `%Y` | 四位年份 | 2026 |
| `%m` | 月（补零） | 09 |
| `%d` | 日（补零） | 11 |
| `%H` | 时，24 小时制（补零） | 15 |
| `%M` | 分（补零） | 30 |
| `%S` | 秒（补零） | 05 |
| `%j` | 一年中的第几天 | 254 |

示例：

```python
import time

time.sleep(1)                      # 暂停 1 秒
print(time.ctime())                # 当前时间字符串
print(time.strftime("%Y-%m-%d %H:%M:%S"))   # 如 2026-09-11 15:30:05

# 用 time.time() 测量代码运行时长
start = time.time()
for i in range(1000000):
    pass
print(time.time() - start)         # 输出耗时（秒）
```

### ==其他常用的标准库模块==

- `os` 与 `pathlib` 模块：这两个模块是处理文件和目录的基础。
  - `os` 模块提供了与操作系统交互的底层接口，
  - `pathlib` 提供了更现代化、更面向对象的路径操作方式
- `datetime` 模块：提供了处理日期、时间和时间间隔的类，是处理时间相关任务的必备工具
- `random` 模块：用于生成各种类型的伪随机数，广泛用于模拟、游戏、随机抽样等场景
- `statistics` 模块：基础统计计算。提供了直接计算描述性统计量（Descriptive Statistics）的函数，如：`mean()` (平均值)、`median()` (中位数)等等
- `math` 模块：基本数学函数。提供了更高级的数学运算，如对数 (`log`)、平方根 (`sqrt`)、三角函数等。在进行数据转换（如对数变换）或实现自定义算法时会用到

