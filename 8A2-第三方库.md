# Python 第三方库

时间：`2026年9月11日`

## 1. 第三方库简介

**定义**

- **标准库**：随 Python 解释器一起安装、开箱即用，如 `turtle`、`os`、`sys`、`colorsys`、`random`
- **第三方库**：由社区/其他组织开发，需要**额外安装**才能使用，如 `numpy`、`requests`、`pandas`、`pygame`
- 存放位置：一般安装在解释器目录的 `Lib\site-packages` 下

**第三方库的安装方式**

- **pip 工具安装**：最常用，直接从 PyPI 下载并安装
- **自定义安装**：通过附加选项定制安装（指定版本、镜像源、安装位置等）
- **文件安装（扩展名 `.whl`）**：先下载 `.whl` 文件，再本地安装

  ```bash
  pip install numpy-1.26.0-cp311-cp311-win_amd64.whl
  ```

  - 适用于网络不畅或需要特定平台/Python 版本包的场景（`.whl` 是预编译的 wheel 包）

## 2. pip 工具（重点）

`pip` 是 Python 的**包管理工具**（第三方库安装工具），**默认自带**（随 Python 一起安装）。

命令格式：

```bash
pip <命令> <库名> <选项>
```

### 2.1 常用命令（在命令行/终端执行，不是在 IDLE 里）

| 命令 | 作用 |
|---|---|
| `pip install <库名>` | 安装库（默认从 PyPI 官方源下载） |
| `pip install <库名>==1.2.3` | 安装**指定版本** |
| `pip install <库名> -U` | 升级到最新版本 |
| `pip uninstall <库名>` | 卸载库 |
| `pip download <库名>` | 只下载不安装（保存到本地，可离线安装） |
| `pip list` | 列出**已安装**的所有库 |
| `pip show <库名>` | 查看某个库的详细信息（版本、位置等） |
| `pip freeze` | 以 `==` 格式列出已安装库（常用于导出依赖清单） |
| `pip help` | 查看帮助 |

- `-r requirements.txt`：按清单文件批量安装
  ```bash
  pip freeze > requirements.txt      # 导出当前环境所有库
  pip install -r requirements.txt    # 在新环境按清单安装
  ```
- `--user`：只安装给当前用户，不写系统目录

### 2.2 镜像源（国内加速）

官方源在国外，下载慢，可用 `-i` 指定国内镜像：

```bash
pip install numpy -i https://pypi.tuna.tsinghua.edu.cn/simple
```

- **`-i` 参数的含义**：`-i` 是 `--index-url` 的缩写，用于**指定下载第三方库的索引地址（镜像源）**。
- pip 默认从国外官方源 `https://pypi.org/simple` 下载，国内访问慢甚至超时；加上 `-i <镜像地址>` 后，pip 改从该镜像服务器下载，达到加速目的（只对本次安装生效）。
- 常用国内镜像源：

  | 镜像源 | 地址 |
  |---|---|
  | 清华大学（最常用，速度快、完整度高） | `https://pypi.tuna.tsinghua.edu.cn/simple` |
  | 阿里云（速度快、完整度高） | `https://mirrors.aliyun.com/pypi/simple/` |
  | 中科大 | `https://pypi.mirrors.ustc.edu.cn/simple/` |
  | 腾讯云 | `https://mirrors.cloud.tencent.com/pypi/simple/` |
  | 华为云 | `https://mirrors.huaweicloud.com/repository/pypi/simple/` |
  | 网易 | `https://mirrors.163.com/pypi/simple/` |

- **永久设置默认源**（免去每次敲 `-i`）：

  ```bash
  pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
  ```

### 2.3 导入与使用

安装完成后，导入方式与标准库完全相同：

```python
import numpy as np          # 惯例别名
from PIL import Image       # 从包中导入子模块
```

**判断一个库是否已安装**

```python
try:
    import numpy
    print("已安装")
except ImportError:
    print("未安装")
```

---

## 3. 常见/基础第三方库简介

### PyInstaller 库（.py → .exe 打包）

#### 简介

- **PyInstaller** 是一个**第三方库**，作用是把 `.py` 源程序**打包成可直接运行的可执行文件**（Windows 下为 `.exe`）
- 打包原理：
  - 把 **Python 解释器 + 你的代码 + 依赖的库文件**捆绑在一起，生成一个不需要用户安装 Python 就能运行的程序
  - Python 是解释型语言，`.py` 文件本身不能直接双击运行；PyInstaller 弥合了"脚本"与"可执行程序"之间的差距

  ```bash
  pip install pyinstaller        
  # 安装（在命令行/终端执行，不是在 IDLE 里）
  ```

#### 基本用法

在**命令行**中进入源文件所在目录后执行：

```bash
pyinstaller -F hello.py
```

- 打包成功后会生成三个目录/文件：`build\`（临时构建文件）、`dist\`（**最终的 exe 在这里**）、`hello.spec`（配置文件）
- 可执行文件在 `dist\` 目录下，把 `build\` 和 `.spec` 删掉也不影响使用
  - 这两个删去的是打包时发挥作用的，不影响可执行文件发挥作用，但是会影响下次打包

#### 常用参数（选择题高频）

| 参数 | 作用 |
|---|---|
| `-F` | 生成**单个** exe 文件（One-File 模式）；不加 `-F` 默认 `-D` 文件夹模式，exe + 一堆依赖文件放在一个目录里 |
| `-D` | 生成**文件夹**（默认方式），启动比 `-F` 快 |
| `-w` | 生成**无控制台窗口**的程序（windowed 模式），适合 GUI 程序；`-c` 则保留控制台 |
| `-i icon.ico` | 指定 exe 的**图标**文件 |
| `-n name` | 指定打包后 exe 的**文件名** |

组合示例：

```bash
pyinstaller -F -w -i myicon.ico hello.py
# 单文件 + 无控制台窗口 + 自定义图标
```

#### 注意事项

- **不跨平台**：在哪个操作系统上打包，就只能生成哪个平台的可执行文件（Windows 上打不出 macOS 的程序）
- 打包命令在**操作系统命令行**中执行，不是在 Python 交互环境/IDLE 里
- `-F` 单文件模式运行时会先解压到临时目录，**启动较慢**；`-D` 文件夹模式启动快但分发麻烦

### NumPy 库（科学计算）

**定位**：全称 **Numeric Python**，Python 科学计算的基础库。核心是多维数组对象 **`ndarray`（N-dimensional array，简称数组）** ，配合向量化运算与广播机制，用一条语句代替多层循环完成批量数值计算。pandas、scipy、matplotlib、scikit-learn 全部构建在其之上。

```python
# 绘制 y = x**2 - 2x + 1 的图像
import numpy as np
import matplotlib.pyplot as plt

x = np.arange(-7, 9, 0.1)      # x 从 -7 到 9，每隔 0.1 取一个点
y = x**2 - 2*x + 1             # 向量化运算：一次算出所有点
plt.plot(x, y)
plt.show()
```

**数组创建**

```python
import numpy as np              # 惯例别名 np

np.array([1, 2, 3])             # 由列表创建，一维
np.array([[1, 2], [3, 4]])      # 嵌套列表创建，二维
np.arange(5)                    # [0 1 2 3 4]，左闭右开，等价于 range
np.arange(1, 10, 2)             # [1 3 5 7 9]
np.arange(-7, 9, 0.1)           # 支持小数步长，range 不支持
np.linspace(0, 1, 5)            # [0. 0.25 0.5 0.75 1.]，均分 5 个点
np.zeros((2, 3))                # 2 行 3 列全 0 的二维数组
np.ones((3, 2))                 # 3 行 2 列全 1 的二维数组
```

| 创建方式 | 说明 |
|---|---|
| `np.array(序列)` | 由列表/元组创建数组，序列的嵌套层次决定维度 |
| `np.arange(start, stop, step)` | 等差数组，**左闭右开**，支持浮点步长 |
| `np.linspace(start, stop, num)` | 在区间内均分 `num` 个点，默认**包含终点**（均分时用，不关心步长） |
| `np.zeros(shape)` / `np.ones(shape)` | 全 0 / 全 1 数组，`shape` 用元组表示 |

- `arange` 与 `linspace` 的区别在于指定条件的类型：给定步长用 `arange`（不保证取到终点），给定点数用 `linspace`（两端都取到）

**索引与切片**

```python
a = np.array([10, 11, 12, 13, 14])

a[0]           # 10，索引从 0 开始
a[-1]          # 14，负索引支持
a[1:4]         # array([11, 12, 13])，切片左闭右开
a[::2]         # array([10, 12, 14])，步长为 2
```

**向量化运算**

```python
a = np.array([1, 2, 3])

a + 2          # array([3, 4, 5])，标量与数组的每个元素运算
a * 2          # array([2, 4, 6])
a ** 2         # array([1, 4, 9])
a > 2          # array([False False  True])，逐元素比较，返回布尔数组

b = np.array([4, 5, 6])

a + b          # array([5, 7, 9])，对应位置元素相加
a * b          # array([4, 10, 18])
```

- `+` 在数组上是**逐元素相加**，不是列表那种首尾拼接
- 数组与标量运算时，标量自动作用到每个元素上，这属于**广播**（broadcasting）机制

### 其他库

**网络爬虫方向**

| 库名 | 描述 |
|---|---|
| requests | 简洁且简单地处理 HTTP 请求。一行 `requests.get(url)` 即可抓取网页内容，是目前最流行的 HTTP 库，适合入门爬虫 |
| scrapy | Web 获取框架，一个半成品爬虫。提供下载、解析、存储的完整流程，只需填写规则即可运行，适合大规模爬虫 |

**数据分析方向**

| 库名 | 描述 |
|---|---|
| numpy | **科学计算库**。提供高性能的多维数组对象 `ndarray` 和向量化运算，是几乎所有 Python 科学计算库（pandas、scipy 等）的底层基础，惯例别名 `np` |
| scipy | 在 numpy 基础上增加了许多库函数。面向科学计算的高级模块：线性代数、积分、优化、统计、信号处理等 |
| pandas | 数据处理、数据分析（数据清洗）。提供 Series 和 DataFrame 两种表格型数据结构，擅长读取 CSV/Excel、缺失值处理、分组聚合，**类比"Python 版 Excel"** |

**文本处理方向**（openpyxl、python-docx 在自动化办公方面特别有用）

| 库名 | 描述 |
|---|---|
| pdfminer | 读取 pdf 数据。能把 PDF 中的文字提取出来（PDF 是版式格式，直接 open 读出的是乱码），常用于文献批量分析 |
| openpyxl | 处理 Excel 表格。读写 `.xlsx` 文件，可操作单元格、公式、图表，自动化办公常用 |
| python-docx | 处理 word 文档。读写 `.docx` 文件，可生成/修改段落、表格、样式，批量排版报告很有用 |
| beautiful soup4 | 解析和处理 HTML、XML。把网页源码解析成可检索的"树"，用 `find()`/`select()` 精准提取标签内容，常与 requests 搭配做爬虫 |

**数据可视化方向**

| 库名 | 描述 |
|---|---|
| matplotlib | **二维图绘制**。Python 最经典的绘图库，折线/柱状/散点/直方图等均可实现，惯例别名 `plt`，pandas 的 `.plot()` 底层也靠它 |
| TVTK | 三维可视化。VTK（Visualization Toolkit）的 Python 封装，面向科学计算的三维数据渲染 |
| mayavi | 更方便的三维可视化（3D 图）。在 TVTK 之上提供更简洁的 API，几行代码即可画出三维曲面、流场等 |

**用户图形界面方向**

| 库名 | 描述 |
|---|---|
| PyQt5 | 用户图形界面开发。Qt 框架的 Python 绑定，控件丰富、跨平台，商业与开源项目使用最广 |
| wxPython | 用户图形界面开发。封装 wxWidgets，用系统原生控件渲染，界面观感接近本地程序 |
| PyGTK | 用户图形界面开发。GNOME 的 GTK 框架绑定，主要在 Linux 桌面环境下使用 |

**机器学习方向**

| 库名 | 描述 |
|---|---|
| scikit-learn | 机器学习。经典"传统机器学习"库：分类、回归、聚类、降维，接口统一（fit/predict），自带教学数据集 |
| TensorFlow | 人工智能。Google 出品的端到端深度学习框架，支持神经网络训练与部署（TensorFlow Lite/JS 等） |
| Theano | 深度学习。较早的数学表达式编译库，能自动求导并把表达式编译成高效代码，对深度学习影响深远（已停更） |

**Web 开发方向**（Flask 可做轻量级 Web 应用）

| 库名 | 描述 |
|---|---|
| Django | Web 框架。"大而全"的代表：自带 ORM、后台管理、认证等，适合完整网站项目 |
| Pyramid | Web 框架。灵活性居 Django 与 Flask 之间，组件按需组装，适合规模可成长的应用 |
| Flask | Web 框架。"轻量级"微框架，核心极简、扩展插件丰富，适合小项目和 API 服务 |

**游戏开发方向**（Pygame 非常有名）

| 库名 | 描述 |
|---|---|
| Pygame | 多媒体制作。基于 SDL 的游戏开发库，处理图像、声音、键盘鼠标输入，教学和小游戏首选 |
| Panda3D | 3D 引擎。卡内基梅隆大学开发的实时三维引擎，支持渲染、物理、角色动画 |
| cocos2d | 2D 引擎。跨平台 2D 游戏引擎（cocos2d-python 为其 Python 版本），常用于独立游戏开发 |

**其它第三方库**

| 库名 | 描述 |
|---|---|
| PIL | 图像处理。Python Imaging Library，支持打开、裁剪、滤镜、格式转换等；现常用其维护分支 Pillow，导入名仍为 `PIL` |
| SymPy | 数学计算。纯 Python 的**符号计算**库：化简、求导、积分、解方程，可当"代数草稿纸"使用 |
| NLTK | 自然语言处理。教学级 NLP 库：分词、词性标注、语料库与文本分析工具齐全（与 jieba 同属文本处理生态） |
| WeRoBot | 微信机器人框架。用几十行代码即可搭建微信公众号自动回复服务 |
| MyQR | 二维码。纯 Python 生成普通二维码，还支持在二维码中嵌入图片 |







