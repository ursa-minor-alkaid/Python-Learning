# 非序列：字典&集合

时间：`2026年9月14日`

## 1. 字典（Dictionary）

### 介绍

- 字典储存的是**键值对（key-value pairs）** 的集合。字典中的每个元素都由一个**唯一的**“键”（key）和一个与之关联的“值”（value）组成。
  - *可以将其类比为现实生活中的字典，其中“键”是单词，“值”是该单词的释义*
  - 通过键可以快速、高效地查找、添加、修改或删除对应的值。
- **主要特点：**
  - 在一个字典中，**所有的键都必须是唯一的**，不允许重复。
  - **键必须是不可变（Immutable）的类型**，例如：字符串（str）、数字（int, float）、元组（tuple）等；**列表（list）或另一个字典（dict）不能作为键**。
  - 值（value）可以是任何 Python 数据类型，如数字、字符串、列表、元组，甚至是另一个字典
- 和 List 相比，Dict 有以下特点
  - 查找和插入的速度极快，不会随着key的增加而变慢
  - 需要占用大量的内存，内存浪费多

### 语法要点

- 创建字典
  - 语法：`dict = {key1 : value1, key2 : value2 }`
    - 字典的每个键值对用冒号 `:` 分割，**每个对之间用逗号 `,` 分割**，整个字典包括在花括号 `{}` 中
    - 可以使用类似格式化 json 的换行写法，便于阅读
  - 创建空字典：`empty_dict = {}`
  - 示例：

    ```python
    student = {
        "name": "张三",
        "age": 20,
        "courses": ["数学", "英语"]
    }
    ```

- 访问字典：通过键值访问；`dict_name[key1]`
- 更新及删除
  - 更新字典：
    - 修改：直接赋值；`dict_name[key1] = new_value`
    - 新增：直接添加没有的键值对；`dict_name[new_key] = value`
    - 注意：Python 3.7+ 的 dict **按插入顺序**保存键值对（遍历、`keys()` 等均为此顺序）；修改已有键的值不改变位置，删除后重新添加会排到最后。顺序**不按键排序**。Python 3.6 之前则与放入顺序无关
  - 删除
    - 删除某个键值对：`del dict_name[key]`
    - 删除字典：`del dict_name`
    - 清空字典：`dict_name.clear`

### 函数/方法

|方法和函数|描述|
|---------|--------|
|len(dict)|计算字典**键值对个数**|
|dict.copy()|返回一个字典的浅复制|
|dict.keys()|返回字典中所有的键，返回的对象类型是 dict_keys|
|dict.values()|返回字典中的所有值，返回的对象类型是 dict_values|
|dict.items()|返回可遍历的(键, 值) 元组数组，返回的对象类型是 dict_items|
|dict.get(key, default)|键存在则返回相应值，否则返回默认值 default（**不会报 KeyError**）|
|dict.pop(key, default)|键存在则**删除**相应键值对并返回该值，否则返回默认值 default|
|dict.popitem()|取出一个键值对，以元组 `(key, value)` 形式返回，同时从字典中删除<br>（Python 3.7+ 按**插入顺序取最后一个**，并非随机）|

**注意：**

- 字典的运算都作用于键**：`in` / `not in`、`min()` / `max()` 判断和比较的都是**键（key）**，而不是值（value）
- dict keys/ values/ items 都是可迭代对象，可以用于循环遍历字典的键、值、键值对，套上 `list()` 可以转换为列表

示例：

```python
d = {"a": 3, "b": 1, "c": 2}

print("a" in d)        # True   —— 查找的是键
print(3 in d)          # False  —— 值不会被查到
print(min(d), max(d))  # a c    —— 按键比较，而非按 value

for key, value in d.items():   # items() 的每个元素是元组，可解包
    print(key, value)
```

---

## 2. 集合（Set）

### 介绍 & 语法

- 集合是一个无序且不包含重复元素的集合。其核心概念源于数学中的集合理论
- **主要特性：**
  - 唯一性：集合最显著的特点是自动去除重复元素。向集合中添加一个已经存在的元素不会产生任何效果
  - 无序性：集合不记录元素的插入顺序。你不能通过索引来访问集合中的元素
  - **元素必须为不可变类型**：与字典的键一样，集合中的元素也必须是不可变的类型
  - 本身的可变性：**集合本身是可变的**，可以随时添加或移除元素
- **set 可以理解为只保存 key 值的字典**
- **创建集合**：两种方式
  - `set()` 函数：从**任意可迭代对象**（列表、元组、字符串、range 等）创建集合；例如 `numbers = set([1, 2, 3, 4, 3, 2, 1])`
  - 花括号字面量：直接写 `{1, 2, 3}`，自动去重，无需 `set()`（空集合除外，见下）
    - `set()` 函数会自动去除重复元素
    - 创建空集合只能使用 `empty_set = set()` 来创建（不能使用 `{}` 创建，这个创建的是空字典）

### 函数/方法

|函数或方法|描述|
|---------|--------|
|s.add(x)|如果数据项 x 不在集合 s 中，将 x 添加到 s|
|s.remove(x)|如果 x 在集合 s 中，移除该元素；**不在则产生 KeyError 异常**|
|s.discard(x)|移除 x，**不在也不报错**（比 remove 更安全的用法）|
|s.clear()|清除 s 中所有的元素|
|len(s)|返回集合 s 的元素个数|
|x in s|如果 x 是 s 的元素，返回 True，否则返回 False|
|x not in s|如果 x 不是 s 的元素，返回 True，否则返回 False|

- 注意：集合**无序**，因此**没有索引访问**（`s[0]` 会报 `TypeError`），成员判断只能靠 `in`
- 集合本身可变，不能作为另一个集合的元素、也不能作为 dict 的键；需要"不可变的集合"时用 `frozenset(s)`

### 集合的运算

两个 set 可以做数学意义上的集合运算，**运算结果都是新集合**（**不改变原集合！**）：

|操作符|描述|等价方法|
|------|----|--------|
|`s \| t`|并集（union，或，OR）：包括 s 和 t 中所有元素|`s.union(t)`|
|`s & t`|交集（intersection）：包括同时在 s 和 t 中的元素|`s.intersection(t)`|
|`s - t`|差集（difference）：包括在 s 中但**不在** t 中的元素|`s.difference(t)`|
|`s ^ t`|对称差集（异或，XOR）：包括 s 和 t 中的**非共同**元素 <br> = 并集减交集|`s.symmetric_difference(t)`|

示例：

```python
a = {1, 2, 3, 4}
b = {3, 4, 5}

print(a | b)   # {1, 2, 3, 4, 5}     并集
print(a & b)   # {3, 4}              交集
print(a - b)   # {1, 2}              差集（在a不在b）
print(b - a)   # {5}                 差集不满足交换律
print(a ^ b)   # {1, 2, 5}           对称差集：(a|b) - (a&b)
```

- 集合运算的典型用途：**去重 + 求共同/差异项**，如两个用户列表的交集（共同好友）、差集（新增/流失用户）
- 比较运算：`s == t` 判断元素是否完全相同；`s <= t` / `s < t` 判断 s 是否为 t 的（真）子集
