# 冒号、缩进和语句顺序

时间：`2026年9月6日`

**冒号和缩进是一种语法。它会帮助 Python 区分代码之间的层次，理解条件执行的逻辑及先后顺序**

<div style="background-color: #e7f3fe; border-left: 6px solid #2196F3; padding: 15px; margin-bottom: 20px;">

- 见[条件语句](./4-条件语句与循环语句.md#1-条件语句)和[循环语句](./4-条件语句与循环语句.md#2-循环语句)
- 见[异常处理](./5-异常处理.md)
- 见[函数](./5-函数.md#func_grammar)
- 见[类](7-面向对象、类.md#类的语法)

</div>

### Python 的执行顺序：默认自上而下

Python 是解释型语言，代码**默认从第一行开始，自上而下逐行执行**。

```python
a = 1          # 第 1 步
b = 2          # 第 2 步
print(a + b)   # 第 3 步
```

改变"直线执行"的手段只有几种：

- **条件语句**：选择执行（满足条件才进入）
- **循环语句**：重复执行（一段代码跑多遍）
- **函数调用**：跳转执行（跳进函数，执行完跳回来）
- **异常处理**：出错时跳转（跳到 `except` 块）

而这些手段，**全部依靠"冒号 + 缩进"来划定范围**。

### 冒号 & 缩进

**冒号：一个代码块的开始**

- `if`、`elif`、`else`、`for`、`while`、`def`、`class`、`try`、`except`、`with` 等**复合语句的末尾必须有冒号 `:`**
- 冒号的含义是："**接下来缩进的那段代码，归我管**"
- 冒号本身不做任何操作，它只是语法标记；忘记写冒号会直接报 `SyntaxError`

**缩进：划分代码块的层次**

- 其他语言多用 `{}` 花括号划分代码块，**Python 只用缩进**，缩进本身就是语法
  - **相同缩进量 = 同一个代码块 = 同一层级**
  - 缩进回退（dedent，回到上一层的缩进量）意味着当前代码块**结束**
- 约定使用 **4 个空格**；**Tab 和空格不能混用**，否则报 `TabError`

**阅读规则**：

1. **平级顺序读**：缩进相同的语句，从上往下依次执行
2. **遇冒号看下一层**：冒号下方缩进的代码块是它的"下属"，只有触发条件满足（if 为真 / 循环未结束 / 函数被调用 / 捕获到异常）才会进入；不满足则整块跳过
3. **缩进回退即结束**：代码块执行完（或被跳过）后，回到上一层级继续向下
4. **嵌套层层递进**：每多一层缩进，从属关系就深一层

### 函数调用：跳转

函数定义（`def` 下方的缩进块）**不会立即执行**，只有被调用时才跳进去；执行完（遇到 `return` 或块结束）再跳回调用处继续向下：

```python
def greet():        # 定义阶段：跳过不执行
    print('你好')

print('开始')        # ①
greet()             # ② 跳入函数执行 print('你好')，然后返回
print('结束')        # ③
```

输出顺序：`开始` → `你好` → `结束`

### 换行与语句分隔：续行符 `\` 与分号 `;`

#### 显式续行：反斜杠 `\`

一行代码太长时，可以在行尾加 `\` 表示"这行没写完，下一行接着写"：

```python
total = 1 + 2 + 3 + \
        4 + 5
# total == 15
```

- `\` 后面**不能有任何字符**（包括注释、空格），否则报错
- 在 `()`、`[]`、`{}` 括号内部换行**不需要** `\`，括号本身可以跨行：

    ```python
    s = ('Python'
        'Java')          # 合法，s == 'PythonJava'
    ```

#### 一行多条语句：分号 `;`

同一行可以写多条简单语句，用 `;` 分隔：

```python
a = 1; b = 2; print(a + b)   # 输出 3
```


