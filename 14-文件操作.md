# 文件操作

时间：`2026年9月10日`

[菜鸟教程](https://www.runoob.com/python3/python3-file-methods.html)

## 1. 文件类型：文本文件与二进制文件

Python 将文件分为两类：**文本文件（Text File）**和**二进制文件（Binary File）**。这是理解所有文件操作的基础，因为两者的读写方式完全不同。

### 1.1 文本文件

- **本质**：**由可读字符（字母、数字、符号、换行符等）组成的序列**
  - **存储形式**：磁盘上仍然是一串字节，但这串字节按照某种**字符编码**（UTF-8、GBK、ASCII 等）与字符一一对应，因此人可以直接打开阅读
  - **典型**：`.txt`、`.py`、`.md`、`.csv`、`.json`、`.html`、配置文件
- **读写特点**：
  - 打开模式不带 `b`，如 `open('a.txt', 'r', encoding='utf-8')`
  - **读出来是 `str`，写进去的也必须是 `str`**
  - Python 会自动做**编码/解码**：写入时 字符串 → 字节，读取时 字节 → 字符串
  - 会做**换行符转换**：Windows 的 `\r\n` 与 Linux/macOS 的 `\n` 在文本模式下会被自动翻译
- **注意**：文本模式**必须关心 `encoding` 参数**。编码不一致会抛出 `UnicodeDecodeError`，这是中文环境下最常见的文件操作报错

### 1.2 二进制文件

- **本质**：纯粹的字节序列（`bytes`），没有"字符"的概念
- **典型例子**：图片（`.png`、`.jpg`）、音频、视频、可执行程序、**Office 文档**、压缩包，以及任何自定义格式的数据文件
- **读写特点**：
  - 打开模式带 `b`，如 `open('a.png', 'rb')`
  - 读出来是 `bytes`，写进去的也必须是 `bytes`
  - **不做**编码/解码，也**不做**换行符转换——字节原样进出
  - 不需要（也不能）指定 `encoding`；人不可直接阅读
- **典型用途**：复制文件、下载/上传图片、读写序列化数据（pickle）

```python
# 二进制方式复制图片：字节原样搬运，不经过任何转换
with open('cat.png', 'rb') as src, open('cat_copy.png', 'wb') as dst:
    dst.write(src.read())
```

**注意：**

- 磁盘上所有文件都是字节。**"文本 vs 二进制"不是存储的区别，而是处理方式的区别**：**文本模式多了一层“编码 ↔ 字符”的解释**，二进制模式则把字节当字节。
- 所以理论上任何文件都能用二进制方式打开（读出来是乱码字节）；但用文本模式打开二进制文件（如图片）可能直接报错，因为字节序列不一定能按指定编码解码成合法字符。

---

## 2. 文件路径：相对路径与绝对路径

### 2.1 绝对路径与相对路径

- **绝对路径（Absolute Path）**：从**根目录**写起的完整路径，位置唯一，不依赖任何上下文
  - Windows：`D:\#Syn-处理中\Python-Learning\14-文件操作.md`
  - Linux/macOS：`/home/errui/notes/14-文件操作.md`
  - 特点：走到哪执行都能找到文件；但换机器/换目录后路径容易失效，不够灵活
- **相对路径（Relative Path）**：相对于**当前工作目录**（CWD, Current Working Directory）的路径
  - 例子：`open('data/a.txt')` —— 打开"当前目录下的 data 文件夹里的 a.txt"

    | 启动方式 | CWD 是什么 |
    |----------|-----------|
    | 终端 `cd` 后运行脚本 | 你 `cd` 到的目录 |
    | IDE（VS Code/Trae/PyCharm）点运行 | 通常是**项目根目录**（工作区），不是脚本所在目录 |
    | 双击 `.py` 文件（Windows） | 往往是脚本所在目录，但不可靠（有些诡异的例外情况） |

  - 特殊符号：`.` 当前目录（`./a.txt`）；`..` 上一级目录（`../config.json`）
  - 注意：**相对路径的"起点"是运行脚本时所在的目录**，不是脚本文件所在目录——这是"本地能跑、换目录就 FileNotFound"的常见原因

    > `script.py` 里写着`open('data/a.txt')`
    > 
    > - 情况一：你在终端先 `cd D:\projects` ，再`python script.py` → CWD 是`D:\projects` ，找`D:\projects\data\a.txt` → 成功
    > - 情况二：你在 `C:\Users\Errui` 下运行`python D:\projects\script.py` → CWD 是`C:\Users\Errui` ，找的是`C:\Users\Errui\data\a.txt` → FileNotFoundError

    ```python
    import os
    os.getcwd()                 # 查看当前工作目录
    os.chdir(r'D:\projects')    # 切换工作目录
    ```

### 2.2 转义字符与斜杠问题

**转义字符**：Python 字符串中 `\` 是**转义引导符**，后面跟特定字母组成特殊字符：

| 写法 | 含义 |
|------|------|
| `\n` | 换行符 |
| `\t` | 制表符（Tab） |
| `\\` | 一个字面反斜杠 `\` |
| `\'` / `\"` | 单/双引号本身 |
| `\r` | 回车符 |

  - **`\n` 与 `\r` 的区别**（来自打字机术语）：`\n`（换行，Line Feed）让纸向上走一行、光标停在当前水平位置；`\r`（回车，Carriage Return）让光标回到**行首**、纸不动。Windows 用 `\r\n`（先回行首再换行）、Linux/macOS 用 `\n`、旧版 Mac 用 `\r`，这就是文本模式要做换行符转换的原因

**文件路径反斜杠问题**

- 问题在于：Windows 路径分隔符**恰好也是 `\`**，于是路径字符串会被误读为转义：

  ```python
  'D:\new\test.txt'   # 实际是 D:<换行>ew<制表>st.txt —— 错误路径！
  ```

- **三种正确写法**：

  ```python
  r'D:\new\test.txt'    # ① 原始字符串：前缀 r 让 \ 不再转义（推荐用于 Windows 路径）
  'D:\\new\\test.txt'   # ② 双反斜杠：每个 \\ 转义成一个字面 \
  'D:/new/test.txt'     # ③ 正斜杠：Windows API 也接受 /，跨平台通用
  ```

  - **`r` 前缀（原始字符串，raw string）**：在引号前加 `r`，告诉 Python"字符串里的 `\` 只是普通字符，不要做转义解释"。因此 `r'D:\new\test.txt'` 中的 `\n`、`\t` 保持字面意义，路径原样保留，最适合写 Windows 路径和正则表达式
  - `r` 前缀的局限：原始字符串**不能以单个 `\` 结尾**（`r'D:\'` 会报语法错误），因为结尾的 `\"` 会被视为转义引号
  - **标准写法使用 `/`**：Python 和 Windows API 都接受 `/` 作为分隔符：
    - `/` 是**跨平台脚本的推荐写法**——既没有转义问题，又不像 `r` 前缀那样有结尾反斜杠的局限
    - 唯一注意：某些 Windows 命令行工具（cmd）用 `/` 表示参数开关，但在 Python 字符串里不受影响

---

## 3. 基本操作：open/ close、读、写

重要区分：

- **不需要导入 os**。`open` 是 Python **内置函数**（built-in），`close`/`read`/`write` 是 `open()` 返回的**文件对象的方法**，开箱即用。
- 只有涉及**文件本身**的操作（删除、改名、列目录、判断存在）时才 `import os`。
  - `open()` 关心"文件里写了什么"（内容）
  - `os` 关心"文件本身存不存在、在哪、叫什么"（文件系统条目）

### 3.1 文件操作：open 与 close

#### `open()`

```python
f = open('a.txt', 'r', encoding='utf-8')
```

- 打开文件，**返回文件对象**
- 语法：`open(file, mode, encoding)`

**常用参数**

- `file`：路径；详见上
- `mode`：打开模式，不带 `b` 为文本模式，带 `b` 为二进制模式

  | 打开模式 | 描述 |
  |----------|------|
  | `'r'` | 只读模式，如果文件不存在，返回异常 `FileNotFoundError`，默认值 |
  | `'w'` | 覆盖写模式，文件不存在则创建，存在则完全覆盖原文件 |
  | `'a'` | 追加写模式，文件不存在则创建，存在则在原文件最后追加内容 |
  | `'x'` | 创建写模式，文件不存在则创建，存在则返回异常 `FileExistsError` |
  | `'b'` | 二进制文件模式 |
  | `'t'` | 文本文件模式，默认值 |
  | `'+'` | 与 `r`/`w`/`x`/`a` 一同使用，在原功能基础上增加同时读写功能 |

- `encoding`：文本模式的编码，如 `'utf-8'`
  - 取值是**字符编码名**（字符串），常用编码：
    - `'utf-8'`：Unicode 可变长编码，全球通用，**推荐默认选择**；中文占 3 字节
    - `'gbk'` / `'gb2312'` / `'gb18030'`：Windows 中文系统传统编码（简体中文），gb2312 ⊂ gbk ⊂ gb18030 范围递增
    - `'ascii'`：仅 128 个英文字符，不能存中文
    - `'utf-16'`：Unicode 双字节编码，带 BOM，Windows 内部 API 常用
  - 不传时：使用 `locale.getpreferredencoding()`，即**系统默认编码**（Windows 中文常为 `gbk`/`cp936`，Linux/macOS 常为 `utf-8`）
    - 跨平台时不显式指定是踩坑根源

#### `close()`/ `with` 语句

```python
f.close()

with open('a.txt', 'r', encoding='utf-8') as f:
    content = f.read()
# 离开 with 块后 f 自动关闭
```
- `close()`：关闭文件，释放资源
- **`with` 语句**：**离开 with 块后**自动关闭文件，即使读取过程中发生异常，也保证文件被关闭
- `as`：别名

### 3.2 读

| 方法 | 作用 | 返回类型 |
|------|------|----------|
| `f.read(size)` | 从文件中读入整个文件内容。参数可选，如果给出，读入前 size 长度的字符串或字节流 | `str`（文本）/ `bytes`（二进制） |
| `f.readline(size)` | 从文件中读入一行内容。参数可选，如果给出，读入该行前 size 长度的字符串或字节流 | `str` |
| `f.readlines(hint)` | 从文件中读入所有行，以每行为元素形成一个列表。参数可选，如果给出，读入 hint 行 | `list[str]` |
| `for line in f:` | 逐行迭代，**最常用、省内存**，适合大文件 | 每次一行 `str` |

- 注意：`read()` 一次性把整个文件载入内存，大文件应改用逐行迭代或 `read(n)` 分块读。

**连续调用 `readline()` 会依次返回多行**

每次 `readline()` 调用后，文件指针自动移到下一行的开头，所以连续调用就逐行往下读：

```python
with open('a.txt', encoding='utf-8') as f:
    line1 = f.readline()   # 第 1 行
    line2 = f.readline()   # 第 2 行
    line3 = f.readline()   # 第 3 行
```

- 返回的内容**包含行尾的 `\n`**，打印前常需 `strip()` 或 `rstrip('\n')`
- 读到文件末尾后，再调用返回空字符串 `''`（不是报错）。因此“读到空就停”是常见循环写法：

```python
while True:
    line = f.readline()
    if not line:      # 读到 ''，即文件末尾
        break
    print(line.strip())
# 等价于：for line in f:
```

### 3.3 写

| 方法 | 作用 | 备注 |
|------|------|------|
| `f.write(s)` | 写入一个字符**串** | 返回写入的字符数；**不自动加换行** |
| `f.writelines(seq)` | 写入字符串序列（如列表） | 同样不自动加换行，需自己 `'\n'.join(...)` |

- **`write()`：一次写一个字符串**

  ```python
  with open('out.txt', 'w', encoding='utf-8') as f:
      f.write('第一行\n')     # 换行符必须自己写
      f.write('第二行\n')
  ```

  - 只接受 `str`（文本模式）或 `bytes`（二进制模式），写数字要先 `str(123)`
  - 写完不自动换行，`f.write('a'); f.write('b')` 的结果是 `ab`

- **`writelines()`：一次写一个列表（或任何可迭代的字符串序列）**

  ```python
  lines = ['apple\n', 'banana\n', 'cherry\n']
  with open('fruits.txt', 'w', encoding='utf-8') as f:
      f.writelines(lines)
  ```

  - 名字里有 `lines`，但它**并不按行处理**——本质是"把序列里的元素挨个 write 进去"，元素之间**不会自动插入换行符**。列表元素自带 `\n` 才换行，否则全部连成一行
  - 更安全的用法：先自己 join 再加换行：

    ```python
    words = ['apple', 'banana', 'cherry']
    with open('fruits.txt', 'w', encoding='utf-8') as f:
        f.writelines(w + '\n' for w in words)   # 生成器逐个补上换行
    ```

- 常见搭配：读取 → 处理 → 写回

  ```python
  with open('a.txt', encoding='utf-8') as src:
      lines = src.readlines()          # 得到 list[str]，每行自带 \n

  with open('b.txt', 'w', encoding='utf-8') as dst:
      dst.writelines(lines)            # 列表直接整体写入
  ```

### 3.4 文件指针与 seek

文件对象内部维护一个**指针（光标）**，标记"下一次读/写从哪里开始"。`read`、`readline` 每执行一次，指针就向后移动；`seek` 可以手动移动指针。

| 名称 | 作用 |
|------|------|
| `f.tell()` | 返回当前文件指针位置（字节偏移量） |
| `f.seek(offset)` | 改变当前文件操作指针的位置。offset 的值：0 为文件开头；1 为从当前位置开始；2 为文件结尾 |

```python
with open('a.txt', encoding='utf-8') as f:
    f.read(5)          # 读 5 个字符，指针移到第 5 个字符之后
    f.seek(0)          # 指针回到文件开头
    content = f.read() # 再次 read 能读到完整内容（否则读到的是剩余部分）
```

- **典型用途**：
  - `f.seek(0)`：读了一遍后想从头再读，不用重新 open
  - `f.seek(0, 2)` 配合 `f.tell()`：快速获取文件大小
- 写入同理：`w` 打开后指针在开头，但原内容已被清空；`a` 打开后指针**自动在末尾**，seek 到中间再写也会被强制移到末尾（追加模式保护已有内容）




