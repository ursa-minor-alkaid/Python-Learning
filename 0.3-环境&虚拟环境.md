# 环境 & 虚拟环境

时间：`2026年9月9日`

[菜鸟教程](https://www.runoob.com/python3/python-venv.html)

## 1. env / venv / `.venv` / `.env` 的区别

四个长得很像的词，分属不同层面的概念，极易混淆。其中只有 ，**env** 与 ，**`.venv`** 则是起源于 Python 社区、由 Python 工具链推广的目录命名约定：

| 名称 | 是什么 | 层面 |
|---|---|---|
| **env** | environment 的缩写，**泛指"环境"** | 概念类别 |
| **venv** | Python 3.3+ **标准库内置的虚拟环境创建工具**（也指用它建出来的环境） | 具体工具 |
| **`.venv`** | 虚拟环境**目录的惯用命名**（带点开头的隐藏目录），内容就是 venv 等工具建出来的环境本体 | 目录名/约定 |
| **`.env`** | 存放**环境变量**键值对的文本文件（配合 `python-dotenv` 使用） | 配置文件 |

- **env**：类别名，不是工具
  - 不能说"使用 env"，只能说"某个 env 是用某个工具（venv / conda / virtualenv / poetry）创建的"
  - 日常口语常混用：有人把 venv 建出来的目录直接命名为 `env/`，此时 `env` 和 `venv` 只是**目录名不同**，本质都是虚拟环境
- **venv**：工具，产出物是一种 env

    ```powershell
    python -m venv myenv      # 创建虚拟环境
    myenv\Scripts\activate    # 激活（Windows）
    source myenv/bin/activate # 激活（Linux / macOS）
    deactivate                # 退出
    ```

    - 它做的事：生成一个**独立的解释器入口 + 独立的 pip + 独立的 `site-packages` 目录**，让不同项目的第三方包互不干扰
    - **venv** 是 Python 专属（标准库模块）
    - 同类竞品：`virtualenv`（第三方，功能更多）、`poetry`、`uv` 等
- `.venv`：一个目录名约定，不是新工具
  - 执行 `python -m venv .venv` 后生成的那个文件夹就叫 `.venv/`，里面装着解释器入口、`Scripts/`（或 `bin/`）、`lib/site-packages/`、`pyvenv.cfg`
  - 前面的点只是让它在文件管理器/`ls` 中默认隐藏，**功能上 `myenv/`、`env/`、`venv/`、`.venv/` 没有任何区别——都只是同一类东西的不同命名**
  - 为什么社区偏爱 `.venv`：
    - 一眼识别"这是虚拟环境，别手动改、别提交"
    - 工具链默认值：`flake8`、`black`、`pylint`、VS Code 的 Python 扩展、`uv venv` 都默认找 `.venv`
  - 惯例：写进 `.gitignore`（虚拟环境体积大且不可移植，靠 `requirements.txt` / `pyproject.toml` 复现）
-  `.env`：**与环境变量有关，和虚拟环境无关**
   - 程序开发中配置的**个人 API Key** 就属于这一类；**密钥字符串放在环境变量里，代码通过 `os.environ` 读取**：

      ```python
      # .env 文件内容（注意：此文件应写入 .gitignore，避免密钥泄露）
      # OPENAI_API_KEY=sk-abc123...

      from dotenv import load_dotenv
      import os

      load_dotenv()                      # 把 .env 中的键值对注入环境变量
      api_key = os.environ["OPENAI_API_KEY"]
      ```

     - 为什么这么约定：**安全**（不硬编码在 `.py` 里）、**隔离**（换账号只改配置不改代码）。
   - 注意区分：
     - `.venv/` 是**目录**，`.env` 是**文件**——两者只差一个字母，作用完全不同。
     - **`.env`** 是跨语言的通用概念（`.env` 惯例起源于 Ruby 的 dotenv，被 Node、Go 等全盘采纳）

### 包含关系一览

```
环境 (environment)
├── 环境变量 (environment variables)     ← PATH、API Key、.env 文件
├── 虚拟环境 (virtual environment)       ← 项目级隔离
│     ├── venv（标准库）→ 产出的目录常命名为 .venv/
│     ├── virtualenv / poetry / uv
│     └── conda 环境（另一套生态）
└── 开发环境 / 测试环境 / 生产环境        ← 工程意义上的"整套条件"
```

---

## 2. Python 中的环境变量与 `.env`

### 2.1 常见的环境变量

| 变量 | 作用 |
|---|---|
| `PYTHONPATH` | `import` 时额外去哪些目录找模块 |
| `API_KEY` 等 | 各服务自定义的密钥/配置，惯例是**全大写 + 下划线** |
| `VIRTUAL_ENV` | 虚拟环境激活时由 `activate` 脚本设置，指向 `.venv/` 目录（见第 3 节"隔离生效的原理"） |

### 2.2 使用环境变量

#### 在 Python 中读写：`os.environ`

```python
import os

# ── 读 ──
path = os.environ["PATH"]           # 键不存在时抛 KeyError
home = os.environ.get("HOME")       # 键不存在时返回 None（推荐）
debug = os.environ.get("DEBUG", "0")  # 键不存在时返回默认值 "0"

# ── 写（只对"当前进程"生效，进程退出即消失；也不会传给父 Shell）──
os.environ["MY_VAR"] = "hello"

# ── 遍历/删除 ──
for k, v in os.environ.items():
    ...
os.environ.pop("MY_VAR", None)
```

#### `.env` 文件：项目级的环境变量

- **动机**：Shell 里设置是"全局"的，而每个项目需要的配置不同（项目 A 用测试 Key，项目 B 用生产 Key）。于是约定：在项目根目录放一个 `.env` 文本文件，随项目走。

  ```bash
  # .env（放在项目根目录）
  OPENAI_API_KEY=sk-abc123...
  DEBUG=true
  DATABASE_URL=sqlite:///local.db
  ```

- 格式规则：
  - `KEY=VALUE`， 一行一条；
  - `#` 开头是注释；
  - 值一般不用引号（含空格时用引号）
- 注意：Python 标准库**不认识** `.env`，需要第三方库 `python-dotenv` 把它"注入"环境变量：

  ```python
  from dotenv import load_dotenv
  import os

  load_dotenv()   # 读 .env，把键值对写入 os.environ（默认不覆盖已存在的变量）

  api_key = os.environ["OPENAI_API_KEY"]
  ```

  - 命令行等价物：`python -m dotenv run -- python main.py`，或较新的 Python 3.13+ 可直接 `python --env-file .env main.py`。
- **安全三件套**（几乎每个项目的标配）：
  1. `.env` → 写进 `.gitignore`（真实密钥绝不入库）
  2. `.env.example` → 提交一份只有键名、没有值的模板，告诉协作者"需要配哪些项"
  3. 泄露即轮换 → 一旦 Key 进了 Git 历史，光删文件没用，必须去服务方**吊销重发**

---

## 3. Python 虚拟环境详解

### 简介

**定义**

- 一个 venv 虚拟环境**是一个普通目录**，内含：

  1. **独立的解释器入口**（`python.exe` 或指向基础解释器的启动器）
  2. **独立的 pip**（装包时装进本目录，而不是全局）
  3. **独立的 `site-packages`**（第三方包的家）

- **虚拟环境 = 给每个项目一个独立的"工具箱"**，项目之间互不干扰
- **不复制**整个 Python 安装，标准库仍然复用基础解释器的那份，所以一个 venv 通常只有十几 MB

**为什么需要虚拟环境**

- Python 装包的默认行为：不借助任何工具时，所有 `pip install` 都装进**同一个全局 `site-packages`**，所有项目共用这一份。随之而来的是：

  | 问题 | 场景 |
  |---|---|
  | **版本冲突** | 项目 A 要 Django 3.2，项目 B 要 Django 5.0——全局只能装一个版本 |
  | **依赖污染** | 试验性安装的包越攒越多，没人敢删，不知道哪个项目还在用 |
  | **不可复现** | 换台电脑，没人说得清"这个项目到底需要哪些包、什么版本" |
  | **权限问题** | 往系统级目录装包，在 Linux/macOS 上常需要 `sudo`，还可能弄坏系统自带的 Python 工具 |

- 因此需要使用虚拟环境进行隔离

### 目录结构

- 创建：

  ```powershell
  cd D:\my-project
  python -m venv .venv     # 按惯例命名为 .venv（见第 1 节消歧）
  ```

- 生成的结构（Windows；Linux/macOS 把 `Scripts\` 换成 `bin/`）：

  ```
  .venv/
  ├── pyvenv.cfg                  ← 虚拟环境的"身份证"
  ├── Scripts/                    ← 可执行文件区
  │   ├── python.exe / pythonw.exe
  │   ├── pip.exe、activate、activate.ps1、deactivate.bat ...
  │   └── （以后 pip 装的命令行工具也出现在这里）
  └── Lib/
      └── site-packages/          ← pip install 的包全落在这里
  ```

- 各内容说明：
  - **`pyvenv.cfg`**：虚拟环境的"身份证"，纯文本配置，内容大致是：

    ```ini
    home = C:\Users\Errui\AppData\Local\Programs\Python\Python312
    include-system-site-packages = false
    version = 3.12.7
    ```

    - `home`：基础解释器的安装路径。venv 里的 `python.exe` 启动时向上找到这个文件，据此定位标准库、判定"我在虚拟环境里"（见下文"隔离生效的原理"）
    - `include-system-site-packages`：是否让全局 `site-packages` 也可见，默认 `false`（完全隔离）；设为 `true` 时虚拟环境能"继承"全局已装的包
  - **`Scripts/` 下的 `activate` 一族**：针对不同 Shell 的激活脚本，`activate.ps1` 给 PowerShell、`activate.bat` 给 CMD、无后缀的 `activate` 给 bash/zsh
  - **`Lib/site-packages/`**：本环境专属的第三方包目录，与全局那份互不可见；这正是"独立 pip"装包的去向

### 隔离"生效"的原理

关键在解释器启动时对自身身份的判定：

1. 运行 `.venv\Scripts\python.exe` 时，它向上发现同级的 `pyvenv.cfg`，于是知道"我在一个虚拟环境里"
2. 据此把 `sys.prefix` 设为 `.venv/`（环境自己的地盘），`sys.base_prefix` 指向真实安装的基础解释器
3. `import` 搜索路径（`sys.path`）里的包目录因此变成 `.venv\Lib\site-packages`，而不是全局那份；`pip` 同理

注意：

- `.venv/` **不进 Git**（体积大、路径写死、不可移植），进 Git 的是 `requirements.txt`
- VS Code / PyCharm 里记得**选择解释器**为 `.venv\Scripts\python.exe`，否则运行和调试用的还是全局 Python

### venv 与 conda 对比

| | venv | conda |
|---|---|---|
| 出身 | Python **标准库**，装完 Python 即有 | 第三方发行版（Anaconda / Miniconda）自带 |
| 隔离对象 | Python 包 | Python 包 **+ 非 Python 依赖**（C 库、MKL、CUDA 等二进制库） |
| 解释器版本 | 不能安装 Python，只能基于**已装好**的解释器建环境 | 可以顺手装任意版本的 Python 本身 |
| 包管理 | `pip` + PyPI | `conda` + 自己的渠道（常与 pip 混用） |
| 体积 | 小（十几 MB 起） | 大（base 环境动辄数 GB） |

选择建议：

- **纯 Python 项目 / Web 开发 / 日常脚本** → `venv`（轻量、标准、工具链默认），追求更快可换 `uv`
- **科学计算、需要 CUDA/地理库等二进制依赖、要同时维护多个 Python 版本** → `conda`（你机器上的 miniconda3 即此路线）
- 二者不必对立：常见做法是 conda 只负责"装不同版本的 Python"，项目隔离仍交给 venv