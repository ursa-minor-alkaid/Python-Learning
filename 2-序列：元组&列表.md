# 序列：元组&列表

时间：`2026年9月14日`

## 1. 列表（list）

### 介绍及语法

**介绍**

- List（列表）是一种有序的集合，可以添加和删除其中的元素
- 列表的作用：避免重复性工作；一行代码存放多个数据

**语法要点**

- 创建列表：
  - `list_name = [datum1, datum2, etc]`
  - 可以创建空列表
  - **列表的数据元素不一定是相同的数据类型**
- 访问列表中的值：通过索引或切片访问，语法见下方[索引与切片](#索引与切片)一节
- 修改及删除
  - 修改：`list_name[index] = new_value`
  - 删除元素：del 语句；`del list_name[index]`

### 索引与切片

列表和元组与字符串共用同一套语法（Cf. [笔记1](./1-基本数据类型和变量.md)的"字符串索引与切片"一节）

- **索引**：`lst[index]`，索引从 `0` 开始；支持**负数索引**，从末尾倒数（`-1` 是最后一个元素）
- **切片**：同字符串
  - `lst[start:end]`，**左闭右开**（包含 start，不包含 end）；
  - 端点都可省略：省略 start 表示从头开始，省略 end 表示取到末尾
  - **带步长的切片**：`lst[start:end:step]`，step 默认为 1；step 为负数时倒着取

      ```python
      nums = [10, 20, 30, 40, 50]
      nums[0] = 99          # 索引赋值：修改单个元素
      print(nums)           # 输出: [99, 20, 30, 40, 50]

      nums[1:3] = [8, 9]    # 切片赋值：一次性替换一整段（两边长度不必相等）
      print(nums)           # 输出: [99, 8, 9, 40, 50]
      ```

- 注意：
  - 越界行为：**索引越界会报 `IndexError`，但切片越界不报错**，只返回能取到的部分
  - **元组使用完全相同的读取语法**（"访问元组：同前列表"指的就是这套规则）；但元组不可变，任何赋值操作（无论是 `t[0] = x` 还是切片赋值）都会引发 `TypeError`
  - **与字符串的关键差异**：列表是可变的，所以可以**通过索引赋值和切片赋值修改列表**，而字符串做不到这一点（不可变）

### 运算符及函数/方法

**列表运算符**

- 列表对 `+` 和 `*` 的操作符与字符串相似。`+` 号用于组合列表，`*` 号用于重复列表
- in/ not in
  - `in`：检查某数据是否在列表中，返回布尔值；例如 `3 in [1, 2, 3, 4]`
  - `not in`：检查某数据是否**不在**列表中，返回布尔值；例如 `5 not in [1, 2, 3, 4]` 为 `True`
- **迭代**：`for variable in list`

**列表函数/方法**

|函数&方法|描述|
|----|----|
|len(list)|列表元素个数|
|max(list)|返回列表元素最大值|
|min(list)|返回列表元素最小值|
|list(seq)|将元组转换为列表|
|list.append(obj)|在列表末尾添加新的对象|
|list.count(obj)|统计某个元素在列表中出现的次数|
|list.extend(seq)|在列表末尾一次性追加另一个序列中的多个值（用新列表扩展原来的列表）|
|list.index(obj)|从列表中找出某个值第一个匹配项的索引位置|
|list.insert(index, obj)|将对象插入列表|
|list.pop |移除列表中的一个元素（默认最后一个元素），并且返回该元素的值|
|list.remove(obj)|移除列表中的一个元素（参数是列表中元素），并且不返回任何值|
|list.reverse()|反向列表中元素|
|list.sort([func])|对原列表进行排序（默认升序，`list.sort(reverse=True)` 改为降序）|

### 列表的赋值与拷贝（引用语义）

- **赋值不复制数据**：把列表赋给一个新变量，只是让两个变量**指向同一个列表**；修改其中一个，另一个会同时变化

    ```python
    a = [1, 2, 3]
    b = a
    b.append(4)
    print(a)    # [1, 2, 3, 4]  ← a 也变了
    print(a is b)  # True，两者是同一个对象
    ```

  - **这只适用于组合数据**（列表、字典、集合等容器类型）。数字、字符串等基本类型赋值后互不影响——因为**它们不可变，“修改”实际是让变量指向新对象**
  - 引用语义&拷贝语义
    - **引用语义** = "赋值传递的是引用（地址），不是数据副本"这种行为的统称

      ```text
      内存图
      a ──┐
          ├──→  [1, 2, 3]   （内存中只有一个列表对象）
      b ──┘
      ```

    - **拷贝语义** = 赋值时真的复制一份数据，两者互不影响（b = a.copy() 就是人为制造拷贝语义）
- **解决办法：用 `copy()` 真正复制一份**

    ```python
    a = [1, 2, 3]
    b = a.copy()    # 等价写法：b = a[:]
    b.append(4)
    print(a)    # [1, 2, 3]  ← 不受影响
    ```

  - *注意：`copy()` 是**浅拷贝**——若列表里嵌套了列表，内层仍是共享的；需要完全独立的副本用 `copy.deepcopy()`*

---

## 2. 元组（tuple）

### 介绍

- 元组（Tuple）与列表（List）在功能上相似，但其根本区别在于可变性。元组一旦完成初始化，其内容便无法再进行任何修改
- **元组的“不可变性”** 具体表现为：
  - **没有修改方法**：元组类型不提供像列表所拥有的 `append()`、`insert()`、`pop()` 或 `remove()` 等会改变自身内容的方法
  - **元素无法重新赋值**：虽然可以通过索引（如 `my_tuple[0]`）来访问元组中的元素，但任何尝试通过索引对元素进行重新赋值的操作（如 `my_tuple[0] = 'new_value'`）都会引发 `TypeError` 异常
- 设计元组的主要原因在于，**其不可变特性为程序带来了更高的代码健壮性与安全性**
  - 当数据集合在程序的生命周期内不应被更改时，使用元组可以从语法层面提供一层保障，有效防止意外的数据篡改
  - 编程实践中的一条重要的指导原则：当一个数据集合代表的是一个固定的、不应被修改的序列时，应当优先选用元组而非列表。
- **实际使用频率**：元组没有列表那么常用。日常代码中很少主动 `tuple1 = (...)` 新建元组，元组更多是**被动出现**——接收函数返回的一组数据（如 `divmod()`、`enumerate()` 的每一项），或来自序列解包（见第 3 节）

### 语法要点

- 创建元组：同列表，只不过是使用 `()`
  - 只包含一个元素时，需要在元素后面添加逗号：`tuple1 = (123,)`；
  - 如果不加逗号，创建出来的就不是元组，而是123这个数。这是因为括号既可以表示元组，又可以表示数学公式中的小括号，这就产生了歧义
  - 多个元素时，最后一个元素末尾的逗号语法上可选，习惯上推荐加
- 访问元组：同前列表
- 修改及删除
  - “修改”元组：如果元组指向了一个list，比如说 `list1 = [1,100]`；这个指向是不变的，但是list1自身的内容是可以改变的
  - 删除元组：元组中的元素值是不允许删除的，但可以使用 `del tuple_name` 语句来删除整个元组

### 运算符及函数/方法

**元组运算符**：同前列表

**元组函数**

|方法|描述|
|----|----|
|len(tuple)|计算元组元素个数|
|max(tuple)|返回元组中元素最大值|
|min(tuple)|返回元组中元素最小值|
|tuple(seq)|将列表转换为元组|

---

## 3. 序列解包（Unpacking）

- 解包：把**可迭代对象**（元组、列表、字符串等）里的元素**一次性拆开**，分别赋值给多个变量
- 决定解包的是赋值语句本身：只要右边是**可迭代对象**，左边放多个变量名就能解包

### 语法要点

- **基础解包**：等号左边写多个变量名，元素按顺序一一对应
  - 左侧变量个数必须与右侧元素个数一致，否则报 `ValueError`

    ```python
    a, b, c = (1, 2, 3)   # 元组解包
    x, y = [10, 20]       # 列表也能解包
    m, n = "ab"           # 字符串也能解包
    print(a, b, c)        # 1 2 3
    ```

- **函数返回多个值**：本质是返回一个元组，接收时通常用解包拆开

    ```python
    def get_info():
        return "张三", 20        # 实际返回 ("张三", 20)

    name, age = get_info()       # 解包接收
    print(name, age)             # 张三 20
    ```

- **`*` 收集剩余元素**：用一个带 `*` 的变量接住多出来的元素（结果是列表）

    ```python
    first, *rest = [1, 2, 3, 4]
    print(first)   # 1
    print(rest)    # [2, 3, 4]

    *head, last = (1, 2, 3)
    print(head)    # [1, 2]
    print(last)    # 3
    ```

- **遍历时解包**：常用于遍历由元组组成的序列（如字典的 `items()`）

    ```python
    d = {"name": "张三", "age": 20}
    for k, v in d.items():
        print(k, v)
    # name 张三
    # age 20
    ```
